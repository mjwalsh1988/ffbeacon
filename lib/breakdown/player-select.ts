/**
 * The players columns the Beacon Breakdown core and the Who Should I Start
 * board both read. ONE copy (plan hazard J): the two used to carry the same
 * string by hand with a comment asking a person to keep them in step.
 */
export const BREAKDOWN_PLAYER_SELECT =
  "id, slug, first_name, last_name, full_name, position, team, status, birth_date, external_ids, metadata, years_experience";
