/**
 * The IDP guide's scoring presets: a re-export of lib/idp, so the guide and
 * the defender profile can never describe the same system two ways.
 */

export { IDP_PRESETS, IDP_PRESET_LABEL, type IdpPresetKey } from "@/lib/idp/scoring-presets";
